return {
  "saghen/blink.cmp",
  opts = {
    keymap = {
      -- Tab selects/accepts the completion item; <CR> stays a plain newline
      -- (the "super-tab" preset has no <CR> mapping, unlike LazyVim's "enter").
      preset = "super-tab",
    },
  },
}
